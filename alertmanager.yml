route:
  receiver: telegram_gateway
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: telegram_gateway
    webhook_configs:
      - url: 'http://${WINDOWS_IP}:8080/alert'
        send_resolved: true